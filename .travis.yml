language: python
python:
  - "2.7"
install:
    - pip install pipenv
    - pipenv install --dev
script: pipenv run py.test --cov=dnstwister --cov-config .coveragerc
after_success:
    - pipenv run coveralls
dist: trusty
sudo: false
